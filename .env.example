# ===================================
# WORD PROPOSAL GENERATOR - CONFIGURATION
# ===================================

# ===================================
# AZURE BLOB STORAGE (Required)
# ===================================

# Storage Account Name
AZURE_STORAGE_ACCOUNT_NAME=your-storage-account-name

# Connection String (for local development only)
# For production, use Managed Identity instead
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=youraccountname;AccountKey=yourkey;EndpointSuffix=core.windows.net

# ===================================
# API SECURITY (Optional but recommended)
# ===================================

# API Key for endpoint protection
# Leave empty to disable authentication (not recommended for production)
API_KEY=your-secret-api-key-here

# ===================================
# APPLICATION SETTINGS
# ===================================

# Server port (default: 8080)
PORT=8080

# Debug mode - shows full responses instead of shortened ones
# Set to 'true' for development, 'false' for production
DEBUG_MODE=false

# ===================================
# IMPORTANT NOTES
# ===================================
# 1. Copy this file to .env and configure your values
# 2. NEVER commit .env file with real credentials to git
# 3. For Azure Container Apps deployment:
#    - Use Managed Identity instead of connection string
#    - Configure environment variables via Azure Portal or deployment script
# 4. Required Azure Storage containers:
#    - word-documents (for generated documents)
#    - word-templates (for templates)
