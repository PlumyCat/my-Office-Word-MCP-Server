# Transport Configuration
# Valid options: stdio, streamable-http, http, sse
MCP_TRANSPORT=stdio

# HTTP/SSE Configuration (when not using stdio)
MCP_HOST=127.0.0.1
MCP_PORT=8000

# HTTP specific (for Azure Container Apps)
MCP_PATH=/mcp

# SSE specific
MCP_SSE_PATH=/sse

# Security (optional)
# MCP_API_KEY=your-secret-api-key

# Logging
FASTMCP_LOG_LEVEL=INFO

# Azure Container Apps Environment Variables
# Set MCP_TRANSPORT=http for Azure deployment

